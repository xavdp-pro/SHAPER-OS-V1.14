# V1.14 — Operational continuity across engines

> **Intent Classification**: GENERIC INTENT (Universal / Parameterized Blueprint)
> **Status:** development work, opened 2026-09-05. Not a release or a cold-test verdict.

## Objective

- Let an authorised agent enter an assigned universe, understand its mission,
  use the available tools, produce externally checkable work and hand it over.
- Preserve the human's ability to shape the system. The kernel informs design;
  the running agent receives the context relevant to its actual mandate.
- Qualify an engine together with its tool harness and environment, on measured
  tasks. A model name or a successful chat response proves no operating capability.
- Keep the base generic; product-specific work remains in its own universe.
- Make interruptions, missing capabilities and unverified results visible.
- Prepare a frozen candidate for a later multi-engine cold campaign.

## Starting point and release identity

The starting commit is `0ef7b8a` on the V1.13 history. Development uses branch
`dev/v1.14`. When this plan was written the package release still read
`1.13.35` and no V1.14 tag existed. Both have moved since: `package.json` and
`software/package.json` read `1.14.0`, and tags `v1.14.0` to `v1.14.4` are
issued. What has *not* moved is the qualification: a tag marks a slice of work,
never a sealed release (`docs/TESTING-REPORT.md` is a V1.13 snapshot), and the
operating contract and the multi-engine campaign below are still owed.

A base or catalogue's root `package.json` declares its release. A checkout's
directory name does not change that release. Historical proofs retain the
version and commit they actually measured. Release preparation must align the
package metadata, manifests and active entry documents before a candidate tag.

## Current development lot

| Work | State | Evidence / next action |
| --- | --- | --- |
| Release checks survive a checkout rename | Implemented locally | Five regression cases, including an unversioned clone and malformed root metadata |
| Proposal identity binds the proposed content | Implemented locally | Changed arguments, authority, lifetime, tool and standing policy are distinguished |
| Standing acceptance follows the catalogue | Implemented locally | Reads and writes require explicit permission; irreversible writes remain manual |
| Context reaches the actual agent backend | Implemented locally | Four context-delivery regressions and a real bridge HTTP transport test |
| Common operating contract and runtime context | Written; live qualification pending | Scope, tools, evidence and recovery information travel in the runtime template |
| Cold multi-engine campaign | Not started | Prepare one fixed candidate, independent terrain and comparable reports |

This table records repository development. It is not a live compatibility matrix.
Test evidence and the remaining limitations are recorded below as work completes.

## Subsequent work, in dependency order

1. **Context and continuity.** Carry current decisions and their reasons, locate
   durable job records and artefacts, and reconcile in-flight work before retry.
   A conversation reset does not undo external effects.
2. **Observed state and recovery.** Join the existing governor, maker and
   supervisor responsibilities; verify fresh observations and bounded remedies.
3. **Proposal execution integration.** Bind authenticated authority to stored
   proposals, retire superseded proposals, and deduplicate transactionally in
   the caller that owns the effects. A digest is not authentication.
4. **A useful product journey.** Prove one complete workflow in a product
   universe and its transfer to another operator. This remains product work.
5. **Release qualification.** Run the common contract against the engines and
   harnesses measured as available, then publish the actual compatibility scope.

## Resume this work

Read this page after the required [agent entry route](../AGENTS.md). Inspect
`git status` and the current diff before changing anything; the working tree
may contain work newer than this table. The [operating contract](agent/OPERATING-CONTRACT.md)
defines the target and the [campaign protocol](agent/MULTI-ENGINE-CAMPAIGN.md)
defines how to test it. Both extend the existing architecture and test protocols.

The active user direction is operational continuity across available engines.
The user intends to launch comparative cold trials after candidate preparation.
Do not turn that future campaign into an unrecorded model benchmark now.

## Validation of this development lot

Observed locally on 2026-09-05 with Node 22.23.2: `npm test` passed **538 tests
in 114 suites**, with no failures or skips. `npm run verify` passed all **8
checks**. The initial regression run failed 10 proposal/version cases on the
unpatched implementations; the four context-delivery cases also failed before
their fixes. The bridge transport test first exposed a missing child cwd, then
proved that only the request, without its context, reached the backend.

The transport test uses the real bridge process and local HTTP with a protocol
recorder. It inspects outgoing request contents and refusals; it does not run a
model or establish model/CLI compatibility. No live SHAPER deployment, cold
image build, model availability measurement, or multi-engine run has yet been
performed for V1.14. Fresh-LXC source verification also passed all 538 tests
without skips and all 8 checks on Debian 13 with Node 20.19.2, as an
unprivileged test user. The dedicated test instance was deleted; existing
instance names and states match the pre-test inventory. See the
[source verification record](V1.14-SOURCE-VERIFICATION.md) for scope and limits.

---

## Does 1.14 need its own catalogue? Measured, 7 September 2026

`SHAPER-OS-BRICKS-V1.14` does not exist. Before cutting one, the question was
whether anything in 1.14 requires it. Measured rather than assumed:

**The base and the catalogue hold disjoint sets.** Not one package is in both.
The base carries the kernel every universe has — runtime, auth, bridges,
governor, logger, maestro, opencode-server, proposal, queue, supervisor,
universe, vault, verify. The catalogue carries what a universe may *add* —
`pkg-db`, `pkg-fleet-dns`, `pkg-ged-engine`, `pkg-mail-agent`, `pkg-rag`, and
the bricks `ged`, `helm`, `mariadb`, `pipeline`, `qdrant`, `waf`.

**Nothing in 1.14 so far touches a contract the catalogue depends on.** The one
contract that changed is task-context resolution (`pkg-maestro/task-paths.js`).
Two apparent hits in the catalogue are not it:

* `brick-helm`'s `contextPath` is a Helm session file, unrelated to the maestro
  task contract — same word, different thing.
* `univ-demo` declares `./tasks/task-schedule.json` and its deploy script sets
  `TASKS_FILE=/data/univ/tasks/task-schedule.json`. That is the `tasks/`
  subdirectory layout, which the new resolution still serves — it is the second
  candidate it tries, which is exactly the behaviour the old code had. Backward
  compatible by construction, and covered by a test built on that layout.

**Therefore**: cut a 1.14 catalogue when a catalogue brick actually changes, or
when the base breaks a contract one of them uses. Not to align a number. The
fleet map keeps naming the 1.13 pair until both halves of 1.14 exist, because a
map that says *base 1.14, catalogue 1.13* would describe an ecosystem that is
not there.

### The catalogue's orientation — settled 7 September 2026

The criterion is not open. Xavier stated it, maintained it when questioned, and
then named it for what it is: **the orientation of this catalogue**. It governs
from here.

> **A catalogue brick is functional and agnostic, so that it can be specialised
> against a client's requirements — and only when that is useful.**

Generic first, specialised on demand. What follows is what that settles, and
what it leaves to work out.

**The line the catalogue's README states is now out of date.** Xavier's line: a catalogue
brick is **functional and agnostic**, so that it can be **specialised later
against a client's requirements**, and only when that is useful. Generic first,
specialised on demand.

The README says the catalogue ships *"what the world already made, packaged to
SHAPER's contracts"* — things that "follow an upstream, not a law". That sorts
by **provenance**. The orientation sorts by **purpose**. They agree on
`brick-mariadb` — `FROM docker.io/library/mariadb`,
genuinely packaged, genuinely agnostic. They disagree elsewhere, and the
catalogue's own contents break the provenance line:

* `brick-helm` is **ours**: `FROM node:20-alpine`, an `app/` directory and an
  `init.sql`. No upstream to follow. It is in the catalogue anyway — and under
  the agnostic-and-specialisable line, correctly so.
* `pkg-fleet-dns` is **pure doctrine**: fractal DNS naming for a fleet of child
  universes, which is Rule 37. Nothing of the world made it.

So the artefact already obeys the orientation, and the README describes a line
the catalogue itself stopped following. Nothing is mis-sorted; a document is out
of date. **Correcting it is 1.14 catalogue work** — the 1.13 catalogue is
published, and a published canon is not retouched.

What the orientation leaves to work out is the other direction:

> A catalogue brick is a generic capability, specialised against a client's
> requirements only when that is useful. Under that criterion, and not under
> provenance: **which packages the base currently carries belong in the
> catalogue?** `pkg-proposal`, the four engine bridges and
> `pkg-opencode-server` sit in the kernel every universe holds, including a
> universe that never speaks to an agent.

That is one hard question, bounded, answerable by reading — the shape §8 of the
inter-agent protocol reserves for the largest model. It cannot be answered in
the 1.13 catalogue, which is published; it is 1.14 work by construction.

**Why the orientation is worth holding**, in the terms this project already
uses: a brick written against one client's requirements can only ever be sold
once, and every later client pays to have it un-specialised first. A generic
brick specialised on demand is the same fractal move as the rest of the system —
one shape, instanced. The specialisation is a declaration on top, not a fork
underneath.
