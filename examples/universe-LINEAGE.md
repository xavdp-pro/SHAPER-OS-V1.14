# LINEAGE — univ-<projet>-<classe>

> **Intent Classification**: GENERIC INTENT (Universal / Parameterized Blueprint)
> Copy to the class repo root as `LINEAGE.md`, before any code (birth step 2).
> A repository whose origin is unknown cannot be maintained (Rule 33.1).

## Cut from

- Base: `SHAPER-OS-V1.13` @ `v1.13.0`
- Catalogue: `SHAPER-OS-BRICKS-V1.13` @ `v1.13.0`

## Why this universe exists

<Two or three sentences. The objective, not the implementation — the
implementation is the manifest's job.>

## Parent

`<univ-projet-classe of the governing universe, or "self" for a standalone
that governs its own ledger — matches governedBy in the fleet map>`

## Consumed bricks

| Brick | Image tag | From |
| :--- | :--- | :--- |
| `brick-vault` | `v1.13.0` | base |
| `brick-...` | `v...` | catalogue |

## Divergences

<Empty at birth. When a brick becomes `source: fork`, its `forkedFrom` lives
in the manifest; write HERE the why — the requirement that forced the
divergence (Rule 33: divergence assumed, never accidental).>
