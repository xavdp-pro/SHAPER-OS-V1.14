# Same as software/.env.example — copy to software/.env from repo root:
#   cp .env.example software/.env

# SHAPER OS — never commit the real file
# Key URLs and tiers: KEYS-AND-ACCOUNTS.md
# The MINIMUM this file must carry, and the start-line gate: docs/PREREQUISITES.md
# Generate vault keys: openssl rand -hex 32  &&  openssl rand -hex 24

VAULT_MASTER_KEY=
VAULT_TOKEN=

DEEPGRAM_API_KEY=
GROQ_API_KEY=
GROQ_ACK_LLM=1
GROQ_ACK_MODEL=groq/compound-mini

# CARTESIA_API_KEY=
# ELEVENLABS_API_KEY=
# OPENROUTER_API_KEY=

# Measured at deployment: run `opencode models`, ping the candidates from the
# target host, and set the cheapest one that answers (Rule 7). Naming one here
# dates this file — the previous default was withdrawn while still shipped.
OPENCODE_MODEL=
BRIDGE_OPENCODE_STUB=0

VAULT_PORT=8610
LOGGER_PORT=8620
OPENCODE_BRIDGE_PORT=4440
OPENCODE_SERVE_PORT=4441
QUEUE_PORT=8640
MAESTRO_PORT=8630
HELM_PORT=8650

APP_MODE=demo
JWT_SECRET=
MAIL_AGENT_STUB=1

# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
