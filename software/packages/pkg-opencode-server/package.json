{
  "name": "@shaper/pkg-opencode-server",
  "version": "1.13.1",
  "private": true,
  "type": "module",
  "description": "HTTP + SSE bridge in front of the OpenCode CLI, on the shared agent-bridge contract",
  "scripts": {
    "start": "./start-bridge.sh",
    "test": "node --test *.test.mjs",
    "test:cli": "bash scripts/test-cli.sh"
  }
}
